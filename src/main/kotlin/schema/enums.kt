package schema

enum class TargetType { post, tasting_note, alcohol }
enum class ReactionTargetType { post, comment, alcohol, tasting_note }
enum class ReactionType { like, unlike }
enum class LocationEnum { /* define your enum values here */ }
enum class PostCategory { free, question }
